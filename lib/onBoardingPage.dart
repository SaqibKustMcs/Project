import 'package:flutter/material.dart';

class OnBoardingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text('this is my first screen')],

        //  Center(child: Text('this is my first Screen')),
      ),
    );
  }
}
